import React from 'react';
import "./Products.css"
const Products = ({product}) => {
    console.log(product)
    return (
        <div>
            
        </div>
    );
};

export default Products;